{
  "name": "baseball-scorebook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal MLB scorecard viewer with sabermetric overlays",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync-tokens": "node scripts/sync-tokens.js --write",
    "export-figma": "node scripts/export-figma-tokens.js",
    "copy-tokens": "cat design-tokens.json | pbcopy && echo 'Copied design-tokens.json to clipboard'"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
